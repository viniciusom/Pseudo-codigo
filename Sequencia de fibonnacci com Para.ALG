algoritmo "Sequencia de fibonnacci com Para"
var
   i, j, k: inteiro
inicio
      j <- 0
      k <- 1
      Para i <- 1 ate 10 faca
           Escreva(j, k)
           j <- j + k
           k <- k + j
      FimPara
fimalgoritmo